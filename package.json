{
  "name": "model-prices",
  "private": true,
  "description": "LLM token pricing as one JSON file",
  "license": "MIT",
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "verify": "node scripts/verify.mjs",
    "generate": "node scripts/generate.mjs"
  }
}
